# junk-drawer

Cheatsheet for the whole marketplace. Run `/junk-drawer` to list every plugin
with its version, description, and commands + params.

The list is generated at runtime from each plugin's `plugin.json` and command
frontmatter (`bin/junk-drawer`), so it never goes stale — add a plugin and it
shows up automatically.
