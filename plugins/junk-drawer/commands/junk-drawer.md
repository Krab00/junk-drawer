---
description: Cheatsheet of every junk-drawer plugin — versions, descriptions, commands + params.
allowed-tools: Bash(${CLAUDE_PLUGIN_ROOT}/bin/junk-drawer:*)
---
Run `${CLAUDE_PLUGIN_ROOT}/bin/junk-drawer` and print its output to the user verbatim inside a code block. Do not summarize, reorder, or reformat it.
