---
description: Install this status line — copies the script and wires it into your settings.json
allowed-tools: Bash
---

!`bash "${CLAUDE_PLUGIN_ROOT}/setup-statusline.sh"`

The status line is now wired into your `settings.json` (backup written next to it). Start a new
session or reload to see it. Nothing else to do.
