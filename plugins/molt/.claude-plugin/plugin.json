{
  "name": "molt",
  "description": "Context-limit orchestrator. Near the token limit, shed the full context (like a snake sheds its skin) into a handoff and continue the work in a fresh session via /clear + auto-restore. Opt-in detector nudges you when you're close.",
  "version": "0.0.3",
  "author": { "name": "Krab00" }
}
